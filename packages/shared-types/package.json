{
  "name": "@safestock/shared-types",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "test:coordination": "pnpm build && node --test tests/coordination.test.cjs",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test:briefing": "pnpm build && node --test tests/briefing.test.cjs",
    "test:inventory-qr": "pnpm build && node --test tests/inventory-qr.test.cjs",
    "test:route-arrows": "pnpm build && node --test tests/route-arrows.test.cjs"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
