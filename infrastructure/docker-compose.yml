services:
  postgres:
    image: postgres:16-alpine
    container_name: ${STACK_NAME:-safestock}_postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-safestock}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-safestock_dev}
      POSTGRES_DB: ${POSTGRES_DB:-safestock}
    ports:
      - "${BIND_ADDRESS:-0.0.0.0}:${POSTGRES_PORT:-5432}:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-safestock}"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: ${STACK_NAME:-safestock}_redis
    restart: unless-stopped
    ports:
      - "${BIND_ADDRESS:-0.0.0.0}:${REDIS_PORT:-6379}:6379"
    volumes:
      - redisdata:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
  redisdata:
