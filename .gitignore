node_modules/
dist/
build/
.next/
# Thư mục build tách biệt cho instance frontend chạy song song (demo/preflight)
# qua NEXT_DIST_DIR — là artifact sinh ra, không commit.
.next-*/
.expo/
*.log
.env
.env.demo
.env.local
.env.*.local
**/.env
!.env.example
!.env.demo.example
coverage/
.turbo/
__pycache__/
*.pyc
.venv/
venv/
.DS_Store
prisma/*.db
*.tsbuildinfo

# OSRM source/graph được build và kiểm checksum riêng; không đưa artifact lớn vào Git.
/infrastructure/osrm/data/*
!/infrastructure/osrm/data/README.md

# Báo cáo xuất cục bộ; dữ liệu có thể chứa thông tin vận hành và được sinh lại khi cần
/apps/backend/baocao-*.xlsx

# Ghi chú làm việc nội bộ (AI memory) — không đẩy lên repo chung
memory/

# Skill AI cài cá nhân (mỗi người tự `npx skills add`) — không commit
.agents/
.claude/
