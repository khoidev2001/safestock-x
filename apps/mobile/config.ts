/**
 * Địa chỉ backend. Expo Web chạy trong trình duyệt trên chính máy → localhost.
 * Nếu test trên điện thoại thật, đổi thành http://<IP-LAN-của-máy>:3100.
 * API_PORT mặc định 3100 (xem .env ở root). Global prefix "/api".
 */
export const API_BASE = "http://localhost:3100";
